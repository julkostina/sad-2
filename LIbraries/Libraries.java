package LIbraries;

/**
 * interface Libraries for creating future libraries
 */
public interface Libraries {
    /**
     * method Draw for drawing elements
     * @param figure for drawing concrete figure
     */
    public void Draw(String figure);
}
